{
  "name": "skills",
  "version": "2.0.0",
  "description": "Self-bootstrapping skill pack — skill-importer and skill-manager manage themselves and the rest of the repo",
  "type": "module",
  "scripts": {
    "check-drift": "node skill-manager/scripts/check-drift.js",
    "check-drift:report": "node skill-manager/scripts/check-drift.js --write-report"
  },
  "engines": {
    "node": ">=18"
  }
}
